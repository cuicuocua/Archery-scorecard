{
  "name": "archery-scorecard",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for the GitHub Pages deployment of ArcheryScorecard.jsx. ArcheryScorecard.jsx itself is the artifact — this package.json only exists to bundle it as a standalone site.",
  "scripts": {
    "build": "node site/build.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "lucide-react": "^0.400.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tailwindcss": "^3.4.0"
  }
}
